function inventoryList(){
	let component = 
	`
	<section id="inventory-list">
		<p class="text-center text-danger empty-inventory-list">Inventory list is empty.</p>
	</section>
	`;
	return component;
}